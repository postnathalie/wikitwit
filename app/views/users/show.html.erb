<% provide(:title, @user.name) %>
<div class="row-fluid">
	<aside class="span4">
		<section>
			<%= image_tag @user.avatar.url(:thumb), class: "avatar" %>
			<h1><%= @user.name %></h1>
		</section>
	</aside>
</div>